# .npmrc — Control optional dependency installation
# We manually install both Linux and Windows rollup native binaries
# because npm's optionalDependencies resolution per-platform overwrites
# the other platform's binary on every install.
optional=false
